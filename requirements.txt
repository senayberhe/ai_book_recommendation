# Runtime dependencies for the deployed app (Hugging Face Spaces reads this file).
# Notebook-only tools (torch, transformers, jupyter, kaggle) are intentionally
# excluded here to keep the Space build fast; see pyproject.toml for the full set.
gradio>=6.18.0
pandas>=3.0.3
numpy>=2.4.6
python-dotenv>=1.2.2
openai>=2.41.1
langchain-core>=1.3.9
langchain-openai>=1.3.2
langchain-chroma>=1.1.0
chromadb>=1.5.9
protobuf>=7.35.1
